# Handoff / recovery note — 2026-06-13 (written mid-session as token-overrun insurance)

> **DATED SNAPSHOT — superseded by `handoff_2026-06-21.md` and `04_empirics/H1_volatility/c1_findings.md`.**
> The "State as of this note" C1 figures below (−16.4%, placebo −13.6%/p=.046, leads p=.033, "naive DiD
> dead") are **pre-fix**; the current headline is DiD **−20.7%** (honest few-cluster p≈0.026/0.046), the
> naive DiD is no longer pre-trend-dead, and the current placebo is a MARGINAL −11.7%. Kept as a record.

## UPDATE (limit hit ~mid-run; resets 10:50am Asia/Calcutta)
Workflow `wf_a98791e0-87d` ran 21 agents (941k tokens) then hit the session limit. Status:
- COMPLETED + journal-cached: find:india2021, find:india2007, all 3 novel designs
  (RF-SC, DCBD-GARCH, FDCD), 2/3 RF-SC audits (both "fixable").
- FAILED (limit): find:vol_basis_methods, verify:* (so bibliography_count=0 — finds exist
  in transcripts but verification never ran), all fetches, critic, 7/9 audits, synthesis.
  find:global + find:causal_methods died on socket errors (retryable).
- NO deliverable .md files were written; papers/ still has only the 3 pre-existing PDFs.
- guarseed413 district pull: DONE 36/36, committed (CEDA-DIST-002, c9452e0).
**Resume (same session, after reset):** `Workflow({scriptPath: <script below>,
resumeFromRunId: "wf_a98791e0-87d"})` — cached agents return instantly, only failures re-run.
**If session lost:** lane results recoverable from agent-*.jsonl in transcript dir below;
or simply re-run the script fresh (finds are cheap to redo; fetchers dedupe via ls).

## In-flight work (check these FIRST in any new session)

### 1. Deep-research workflow (ultracode) — run `wf_a98791e0-87d`
- **What:** ban-period literature sweep (5 lanes) + citation verification + PDF archiving
  + completeness critic + 3 novel-method designs with 3-lens hostile correctness audits
  (identification / inference-at-our-sample-sizes / prior-art) + synthesis.
- **Deliverables it writes to disk (check existence/completeness):**
  - `01_literature/ban_literature_review.md` (annotated bibliography by episode)
  - `01_literature/methodology_menu_c1_c2.md` (ranked C1/C2 estimator menus + novel
    designs w/ audit verdicts; everything marked DECISION NEEDED for the researcher)
  - `01_literature/references.md` (appended, existing format preserved)
  - `01_literature/papers/*.pdf` (archived sources, `<Author><Year>_<slug>.pdf`)
- **Script (persisted):**
  `~/.claude/projects/-Users-narayan-Desktop-agri-ban-project/2f79d294-7344-4d9a-b8c0-5eaeeb58f474/workflows/scripts/ban-methodology-research-wf_a98791e0-87d.js`
- **Agent transcripts (raw results if it dies mid-run):**
  `~/.claude/projects/-Users-narayan-Desktop-agri-ban-project/2f79d294-7344-4d9a-b8c0-5eaeeb58f474/subagents/workflows/wf_a98791e0-87d/agent-*.jsonl`
- **Recovery:** same session → `Workflow({scriptPath, resumeFromRunId:"wf_a98791e0-87d"})`
  (completed agents cached). New session → re-run the script file fresh; PDF fetchers
  `ls` papers/ first so downloads are not redone; or hand-salvage lane results from the
  agent JSONLs above. After it finishes: review both .md files, commit, update session_log.

### 2. CEDA guarseed413 district pull (background, resume-safe)
- 36 state-files target → `02_data/raw/agmarknet/district/guarseed413__state*.json`
  (8 done at last check; quota 40 req/hr).
- **Recovery:** just rerun `python3 00_code/download_ceda_agmarknet.py` (skips existing).
- **When complete:** append data_log row (CEDA-DIST-002 style), commit raw, rerun
  `00_code/build_volatility_panel.py` (picks up 413 automatically), rerun
  `run_v0_did.py vol_panel_monthly.csv district guar` + `run_v0_placebo.py` as
  robustness re-pass (headline should not change: naive DiD dead by placebo+leads).

## State as of this note (all committed through `16772d2`)
- V0: national DiD null; district DiD −16.4% (p=.029) but placebo (−13.6%, p=.046) and
  joint lead test (p=.033) kill naive DiD per pre-registered rules → synthetic control
  becomes primary. GARCH stable only on clean staples; `_distmed` variant exists.
- Guar control fixed: CEDA id 413 (corr .987 vs futures) replaces gum-contaminated id 75.
- MCX cotton: both generations raw (MCX-002/003) + c1–c3 constructed (MCXC-004/005),
  unspliced (bale vs candy units = flagged decision).
- cv_story.tex Results section current through the falsification findings.

## Researcher decisions pending (rule 6 — not Claude's)
1. Ratify: naive DiD dead → synthetic-control family primary for C1.
2. Donor list: castor/jeera strong; turmeric ok; guar→id413; cotton doubly contaminated
   (own Aug-2022 halt + unit break); kapas out (trades 20–45% of days).
3. Cotton bale↔candy splice rule (if cotton kept at all).
4. Methodology menu choices once `methodology_menu_c1_c2.md` lands.

## Still owed by researcher (blocks C2 basis work)
- chana + wheat Investing.com daily files (c1–c3), FCPO (Bursa) — the last data gap in
  the C1/C2 interview story (GS interview prep: deep mastery C1/C2, overview C3).
